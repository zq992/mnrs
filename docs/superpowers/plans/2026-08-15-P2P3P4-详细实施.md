# P2/P3/P4 玩法重设计 · 详细实施计划

> 状态：**已获批**（2026-08-15）。执行顺序 **P2 → P3 → P4**（2026-08-15 调整：先做 P2 婚姻，P3/P4 下次继续）。
> ⚠ **强制优先（2026-08-15 用户指令）**：无论用户下次提出什么新需求，**必须先把本计划剩余任务（P3-2 → P3-3 → P3-4 → P4-1 → P4-2）全部执行完**，再处理新需求。
> 已确认：P2-3b 做完整版；受封/举贤新资源用新增字段。
> 颗粒度对齐 `2026-08-15-P1基础修复.md`。逐任务完成时更新 checkbox。

## 主战场与验证

- 文件：`scripts/ui/hud.gd`、`scripts/core/character_manager.gd`、`scripts/core/event_manager.gd`、`scripts/core/game_state.gd`、`resources/events/西周/events.json`。
- 验证：`./Godot_v4.3-stable_win64.exe --headless --path . --quit-after 10 2>&1 | grep -iE "ERROR|Parse|Compile"` 须 0 报错；关键任务跑 `res://tests/test_gameplay.tscn` 冒烟。不用 `--check-only --script`。
- 约束：中文注释；不新增美术资源；冷却 ID 稳定非空；旧档 `.get()` 兜底；无后世词（"公"是周王尊称非阶梯顶点）；git 只提交本地。

---

## 〇、新字段与资源模型（先落地）

角色级（挂 current_character，自动存档）：`king_favor`(王宠 0-100，士20/卿30/诸侯40)、`military_merit`(军功)、`regency_power`(实权 0-100)、`noble_title`(子/男/伯/侯/公)、`noble_level`(0-5)、`level_start_year`(资历=current_year−该值)。
family_data：`intel`(情报)、`lovers`(固定情人列表)。

初始化：`create_character`（cm:176）+`create_heir_character`（cm:1713）。**顺带修 birth_state bug**：`create_character` 加 `"birth_state": options.get("birth_state", "周王畿")`（此前 options 值被丢弃→受封永不回乡就封）。继承：heir 拷贝 `fief`/`official_position`/`birth_state`/`noble_title`/`noble_level`，`king_favor=int(父王宠/3)`、军功保留、`level_start_year=current_year`。

---

## P3-1 关闭晋升后门（40k-70k）✅ 已完成（4d1cf3e）

> 前台测试手册（玩家手把手版）：`docs/superpowers/tests/2026-08-15-P3-1-前台测试.md`

- [x] `CharacterManager.get_tenure_years(char)` + `tenure_requirement(level)`（奴1/庶2/士3/卿5/诸侯8）
- [x] `_on_power_promote`（hud:3851）：加资历门槛，不满足拒绝
- [x] `_on_seek_promotion`（hud:3968）：tier0/1 改加王宠+声望，不再 promote
- [x] `_on_plot_execute`（hud:4037）：tier0/1 改"操控国政"（实权+野心），不再 promote
- [x] `_on_reputation_promote`（hud:3862，死代码）：改只加声望
- [x] `_handle_father_inheritance`（hud:1385）：父诸侯时顺带传 `fief`

## P3-2 晋升之门面板（50k-90k）
- [x] `_show_promotion_gate()`：四道门资格可视（世袭继承人/举贤checklist/受封三条量条/历史事件倒计时）
- [x] HUD 家族传承条（家声/爵位/距下事件年）
- [x] 替换三处"请迁"按钮为单个"晋升之门"

## P3-3 四道门核心逻辑（120k-220k，最重）
- [x] `grant_promotion(char)`：从 promote_character 提取，去 can_promote 门槛，保留封地/家兵/衰减/停滞重置/level_start_year
- [x] 世袭：create_heir 补拷贝 + `_do_hereditary_succession`，`_handle_father_inheritance` 改接
- [x] 举贤（士→卿）：声望≥60+技能≥3+大比兴贤窗口，失败累计乡望 4-6 次必成；events.json 加大比兴贤事件
- [x] 受封（卿→诸侯）：声望≥120+势力≥60+军功≥3+王宠≥60，册命大典事件（once 待 P4-1 启用），王宠-30，base30%上限85%
- [x] 资历门：各门前置 `tenure>=requirement`
- [x] 国人生计线：耕作（声望+石）、从军（+军功）

## P3-4 爵位线（60k-100k）
- [ ] `advance_noble_title(char)`：王宠≥40+声望≥80 朝觐大典掷骰晋爵，王宠-20
- [ ] 受封诸侯初始爵位（军功≥3或声望≥120→子爵，否则男爵）；"公"仅特殊事件
- [ ] HUD 身份行加 `| 爵位：%s`

## P4-1 事件机制扩展（40k-70k）
- [ ] `_check_prerequisites` 加 min_year/max_year/year_range/min_power/min_king_favor/min_military_merit/min_skill/has_official_position/social_level
- [ ] once-ever 守卫（启用 completed_timeline_events，game_state:51）
- [ ] `_apply_effects` 加 king_favor/military_merit/regency_power/noble/promote/demote/fief_set
- [ ] 条件式结局（resolution.outcomes 带 condition）
- [ ] 修 era_progress 死更新（TimeManager 年份+1 后调 `_update_era_progress`）

## P4-2 全朝代事件铺排（50k-90k）
- [ ] 三监之乱(前1042±3)/周公还政(前1036-1032)/成康册命(前1005-1000)/昭王南征(前982-977)/厉王国人暴动共和(前841-828)/幽王亡国(前782-771)；均 once+year_range；每代 1-2 窗口

## P2-1 妻妾交互统一（40k-70k）✅ 已完成
- [x] `start_pregnancy` 加 `force_success` 参；新增 `_do_try_for_baby(mt,idx)` 统一判定（tier0 必孕）
- [x] `_get_family_actions` 补 furen/ying_qie/tongfang 分支 + 家族面板列表补齐（媵妾/夫人/通房可达）

## P2-2 忠诚模糊显示（40k-70k）✅ 已完成
- [x] `loyalty_band(l)`（≥80忠诚/≥60尚可/≥40有隙/<40离心）
- [x] 妾室/配偶/媵妾/夫人/通房行改模糊档位
- [x] `test/boost_spouse_loyalty` 加 mother_type/index 参数
- [x] 妾室补"考察忠诚/安抚博欢心"

## P2-3b 忠诚动态化+情人系统（120k-200k，完整版）
- [x] 每季忠诚衰减 1-2（floor 20，本季互动过不衰减）；<50 出轨概率抬升
- [x] family_data.lovers 固定情人列表（`_generate_affair_candidates` 生成后存）
- [x] 情人 3 动作：私会/赠礼/温存→亲密+风声
- [x] 风声≥15 败露：丑闻+和睦降+报复/和离
- [x] 偷情收益：intel 情报+1-2，HUD 显示

## 汇总验收

端到端：无头 0 报错 → test_gameplay 冒烟 → 手工走查（资历请迁/暗谋只涨实权/觐见只涨王宠/晋升之门资格/受封得诸侯+爵位/历史事件窗口 once/忠诚档位/同房判定统一/情人跨季+风声败露）。
提交：每任务独立 commit，仅本地。
